const { expect } = require('chai');

describe('Realizando testes - PRODUCT MODEL:', function () {
  it('Recuperando lista de passageiros com sucesso', async function () {
    expect(true).to.be.equal(true);
  });
});